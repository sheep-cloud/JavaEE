server:
  port: 3344
  
spring:
  application:
    name: spring-cloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:colg-cloud/spring-cloud-config.git                  # GitHub上面的git仓库地址
