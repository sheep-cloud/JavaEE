<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.colg.dao.DepartmentMapper">

	<select id="findById" resultType="cn.colg.entity.Department">
		SELECT dd.id, dd.dept_name 'departmentName'
		FROM tbl_dept dd
		WHERE dd.id = #{id}
	</select>
	
	<!-- 
		根据id查询部门，把员工也查询出来
	 -->
<!-- 	 <select id="findByIdPlus" resultMap="">
		SELECT
			td.id, td.dept_name,
			tl.id, tl.last_name, tl.gender, tl.email, tl.dept_id
		FROM tbl_dept td
		INNER JOIN tbl_employee tl ON td.id = tl.id
		WHERE td.id = #{id}
	 </select> -->

</mapper>